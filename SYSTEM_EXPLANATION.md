# 📚 คู่มืออธิบายระบบ HaaTee ทั้งหมด

## 🎯 ภาพรวมระบบ

**HaaTee** เป็นแพลตฟอร์มอสังหาริมทรัพย์ที่สร้างด้วย **React + Vite** ประกอบด้วย 3 บทบาทหลัก:
- **ผู้ซื้อ/ผู้เช่า (Buyer)**: ค้นหา บันทึก และติดต่อเจ้าของทรัพย์
- **เจ้าของทรัพย์/นายหน้า (Seller)**: ลงประกาศ จัดการทรัพย์สิน และสร้างสัญญา
- **ผู้ดูแลระบบ (Admin)**: จัดการผู้ใช้ ทรัพย์สิน รายงาน และระบบทั้งหมด

---

## 🛠️ เทคโนโลยีที่ใช้

### Frontend Framework
- **React 19.2.0** - UI Framework
- **Vite 7.2.2** - Build Tool (เร็วกว่า Create React App)
- **lucide-react** - Icon Library
- **recharts** - Chart Library (สำหรับกราฟสถิติ)

### การจัดการ State
- **React Hooks** (useState, useEffect, useCallback, useMemo)
- **localStorage** - เก็บข้อมูลผู้ใช้, แชท, ทรัพย์สินที่บันทึก
- **sessionStorage** - เก็บสถานะหน้าเว็บ, พารามิเตอร์การค้นหา

### ข้อมูล (Data Storage)
- **JSON Files** (Static Data):
  - `src/data/properties.json` - ข้อมูลทรัพย์สินทั้งหมด
  - `src/data/users.json` - ข้อมูลเจ้าของทรัพย์/นายหน้า
  - `src/data/buyers.json` - ข้อมูลผู้ซื้อ
  - `src/data/reports.json` - รายงานปัญหา
  - `src/data/chatMessages.json` - ข้อความแชท

---

## 📁 โครงสร้างโปรเจค

```
src/
├── App.jsx              # Component หลัก จัดการ routing และ state
├── main.jsx             # Entry point
├── pages/
│   ├── Home.jsx        # หน้าหลัก (ก่อน login)
│   ├── Login.jsx       # หน้าเข้าสู่ระบบ
│   ├── Register.jsx    # หน้าสมัครสมาชิก
│   ├── Properties.jsx  # หน้าค้นหาทรัพย์สิน
│   ├── Buyer/          # ระบบผู้ซื้อ
│   │   ├── Buyer.jsx
│   │   ├── SearchResults.jsx
│   │   └── PropertyDetail.jsx
│   ├── Seller/         # ระบบเจ้าของทรัพย์
│   │   └── Seller.jsx
│   └── Admin/          # ระบบผู้ดูแล
│       └── Admin.jsx
├── data/               # ข้อมูล JSON
├── styles/             # CSS Files
└── utils/              # Utility Functions
```

---

## 🔐 ระบบ Authentication (การยืนยันตัวตน)

### หลักการทำงาน

1. **หน้า Login** (`src/pages/Login.jsx`)
   - เลือกประเภทผู้ใช้: Buyer, Seller, Admin
   - ตรวจสอบอีเมล/รหัสผ่าน
   - ส่ง OTP (รหัส 6 หลัก) - **ทดสอบใช้: 123456**
   - หลังยืนยัน OTP สำเร็จ → บันทึกข้อมูลลง `localStorage`

2. **การเก็บข้อมูล Login**
   ```javascript
   localStorage.setItem('haatee_login_state', JSON.stringify({
     email: 'user@email.com',
     type: 'buyer' | 'seller' | 'admin',
     role: 'Buyer' | 'seller' | 'System Administrator',
     lastLogin: '2025-01-15 10:30:00'
   }));
   ```

3. **การตรวจสอบ Login State**
   - `App.jsx` โหลดข้อมูลจาก `localStorage` เมื่อเริ่มต้น
   - ถ้ามี login state → redirect ไป dashboard ตาม type
   - ถ้าไม่มี → อยู่หน้า Home

### Credentials สำหรับทดสอบ

**Admin:**
- Email: `admin@haatee.com` หรือ `admin123`
- Password: `admin123456`
- OTP: `123456`

**Seller:**
- Email: `damrong@haatee.com` (หรืออีเมลอื่นใน users.json)
- Password: `seller123456`
- OTP: `123456`

**Buyer:**
- Email: `buyer@haatee.com` (หรืออีเมลอื่นใน buyers.json)
- Password: `buyer123456`
- OTP: `123456`

---

## 🏠 ส่วนหน้าเว็บทั่วไป (ก่อน Login)

### 1. หน้า Home (`src/pages/Home.jsx`)

**ฟีเจอร์:**
- Hero Section พร้อมสไลด์รูปทรัพย์สิน
- ฟอร์มค้นหา: ทำเล, ประเภท, ราคา
- แสดงทรัพย์สินเด่น (Featured Properties)
- ประเภททรัพย์สิน (Categories)
- ฟีเจอร์ของแพลตฟอร์ม
- Footer

**การทำงาน:**
- โหลดข้อมูลจาก `properties.json`
- คลิกทรัพย์สิน → ไปหน้า `PropertyDetail`
- คลิกค้นหา → บันทึกพารามิเตอร์ใน `sessionStorage` → ไปหน้า `Buyer` (แสดงผลการค้นหา)

**State Management:**
```javascript
const [searchLocation, setSearchLocation] = useState('');
const [searchType, setSearchType] = useState('');
const [searchPrice, setSearchPrice] = useState('');
```

### 2. หน้า Properties (`src/pages/Properties.jsx`)

- แสดงรายการทรัพย์สินทั้งหมด
- ฟิลเตอร์: ประเภท, ราคา, ทำเล
- คลิกทรัพย์สิน → ไปหน้า `PropertyDetail`

### 3. หน้า PropertyDetail (`src/pages/PropertyDetail.jsx`)

- แสดงรายละเอียดทรัพย์สิน
- รูปภาพ, ข้อมูล, สิ่งอำนวยความสะดวก
- ปุ่ม "ติดต่อเจ้าของ" → ต้อง login
- ปุ่ม "บันทึก" → ต้อง login

---

## 👤 ระบบผู้ซื้อ (Buyer)

### หน้าที่หลัก (`src/pages/Buyer/Buyer.jsx`)

**Tabs:**
1. **Browse** - เรียกดูทรัพย์สิน
2. **Saved** - ทรัพย์สินที่บันทึกไว้
3. **Chat** - แชทกับเจ้าของทรัพย์
4. **Profile** - โปรไฟล์ผู้ใช้

### ฟีเจอร์สำคัญ

#### 1. การค้นหา
- ค้นหาด้วยคำ (ทำเล, ชื่อโครงการ)
- ค้นหาตามสถานี BTS/MRT
- ฟิลเตอร์: ประเภท, ราคา, ทำเล
- ผลการค้นหาบันทึกใน `sessionStorage`

**การทำงาน:**
```javascript
// บันทึกพารามิเตอร์การค้นหา
sessionStorage.setItem('homeSearchParams', JSON.stringify({
  location: 'สาทร',
  type: 'คอนโด',
  price: '3-5m',
  fromHome: true
}));

// Buyer.jsx โหลดและกรองข้อมูล
const searchParams = JSON.parse(sessionStorage.getItem('homeSearchParams') || '{}');
const filteredProperties = propertiesData.filter(p => {
  // กรองตาม searchParams
});
```

#### 2. การบันทึกทรัพย์สิน (Saved Properties)
- คลิกปุ่ม Heart → บันทึกทรัพย์สิน
- เก็บใน `localStorage`:
  ```javascript
  localStorage.setItem(`savedProperties_${buyerEmail}`, JSON.stringify([propertyId1, propertyId2]));
  ```

#### 3. ระบบแชท
- แชทกับเจ้าของทรัพย์
- เก็บข้อความใน `localStorage`:
  ```javascript
  localStorage.setItem(`buyer_chat_${propertyId}_${sellerId}`, JSON.stringify(messages));
  ```
- รองรับการส่งรูปภาพและไฟล์

#### 4. สัญญาดิจิทัล (E-Contract)
- รับสัญญาจาก Seller
- ดูสัญญา, ดาวน์โหลด PDF
- ลงนามสัญญา (จำลอง)

**การเก็บข้อมูล:**
```javascript
localStorage.setItem(`contracts_${buyerEmail}`, JSON.stringify([
  {
    id: 'contract_1',
    propertyId: 123,
    sellerId: 456,
    monthlyRent: 45000,
    leaseDuration: 12,
    status: 'pending' | 'signed' | 'completed'
  }
]));
```

#### 5. โปรไฟล์
- แก้ไขข้อมูลส่วนตัว
- ตั้งค่าการแจ้งเตือน
- ตั้งค่าความเป็นส่วนตัว
- เปลี่ยนรหัสผ่าน

**การเก็บข้อมูล:**
```javascript
localStorage.setItem(`profile_${buyerEmail}`, JSON.stringify({
  firstName: 'สมชาย',
  lastName: 'ใจดี',
  email: 'buyer@haatee.com',
  phone: '089-123-4567',
  profileImage: 'base64...',
  bio: '...'
}));
```

---

## 🏢 ระบบเจ้าของทรัพย์ (Seller)

### หน้าที่หลัก (`src/pages/Seller/Seller.jsx`)

**Tabs:**
1. **Dashboard** - สถิติและภาพรวม
2. **Listings** - จัดการประกาศทรัพย์สิน
3. **Analytics** - วิเคราะห์ประสิทธิผล
4. **Chat** - ข้อความจากผู้สนใจ
5. **Contracts** - สัญญาดิจิทัล
6. **Profile** - โปรไฟล์

### ฟีเจอร์สำคัญ

#### 1. Dashboard
- สถิติ: ประกาศที่ใช้งาน, ยอดดู, ยอดสนใจ, ยอดติดต่อ
- กิจกรรมล่าสุด
- ประกาศชั้นนำ

**การคำนวณสถิติ:**
```javascript
const stats = {
  activeListings: listings.filter(l => l.status === 'active').length,
  totalViews: listings.reduce((sum, l) => sum + l.views, 0),
  totalSaves: listings.reduce((sum, l) => sum + l.saves, 0),
  totalContacts: listings.reduce((sum, l) => sum + l.contacts, 0)
};
```

#### 2. จัดการประกาศ (Listings)
- **เพิ่มประกาศใหม่**: กรอกข้อมูล → บันทึกใน state
- **แก้ไขประกาศ**: แก้ไขข้อมูล → อัปเดต state
- **ลบประกาศ**: ลบจาก state
- **รีโพสต์**: เปลี่ยนสถานะจาก expired → active

**การโหลดข้อมูล:**
```javascript
// โหลดจาก users.json และ properties.json
const sellerEmail = localStorage.getItem('sellerEmail');
const seller = usersData.find(u => u.email === sellerEmail);
const myProperties = propertiesData.filter(p => p.sellerId === seller.id);
```

#### 3. Analytics
- กราฟแสดงยอดดู, ยอดสนใจ, ยอดติดต่อ
- เลือกช่วงเวลา: 7 วัน, 14 วัน, 1 เดือน, 3 เดือน
- สัดส่วนประเภททรัพย์สิน
- คำแนะนำเพื่อเพิ่มประสิทธิผล

#### 4. ระบบแชท
- รับข้อความจากผู้ซื้อ
- ส่งข้อความตอบกลับ
- เก็บข้อความใน `localStorage`:
  ```javascript
  localStorage.setItem(`seller_chats_${sellerId}`, JSON.stringify({
    'property_123': {
      propertyId: 123,
      buyerName: 'สมชาย ใจดี',
      buyerEmail: 'buyer@email.com',
      messages: [
        { id: 1, sender: 'buyer', text: '...', time: '10:30' },
        { id: 2, sender: 'seller', text: '...', time: '10:35' }
      ]
    }
  }));
  ```

**การสร้างแชทเริ่มต้น:**
- `src/utils/generateSellerChats.js` สร้างข้อความเริ่มต้นให้ Seller ทุกคน
- เรียกใช้ครั้งเดียวเมื่อเริ่มแอป (ใน `App.jsx`)

#### 5. สัญญาดิจิทัล (E-Contract)
- **สร้างสัญญาใหม่**:
  1. เลือกทรัพย์สิน
  2. กรอกข้อมูลผู้เช่า
  3. กำหนดเงื่อนไข (ค่าเช่า, ระยะเวลา, เงินมัดจำ)
  4. ส่งสัญญาให้ผู้เช่าผ่านแชท

- **บันทึกร่างสัญญา**: บันทึกไว้แก้ไขภายหลัง
- **ดูสัญญาที่เสร็จสมบูรณ์**: ดาวน์โหลด PDF

**โครงสร้างข้อมูลสัญญา:**
```javascript
{
  id: 'contract_1',
  propertyId: 123,
  tenantId: 456,
  firstName: 'สมชาย',
  lastName: 'ใจดี',
  monthlyRent: 45000,
  leaseDuration: 12,
  deposit: 3, // เดือน
  startDate: '2025-02-01',
  conditions: 'ห้ามเลี้ยงสัตว์',
  status: 'draft' | 'sent' | 'signed' | 'completed'
}
```

---

## 👨‍💼 ระบบผู้ดูแล (Admin)

### หน้าที่หลัก (`src/pages/Admin/Admin.jsx`)

**Tabs:**
1. **Dashboard** - ภาพรวมระบบ
2. **Users** - จัดการผู้ใช้
3. **Properties** - จัดการทรัพย์สิน
4. **Reports** - รายงานปัญหา
5. **Contracts** - สัญญาทั้งหมด
6. **Analytics** - สถิติระบบ
7. **Settings** - ตั้งค่าระบบ

### ฟีเจอร์สำคัญ

#### 1. Dashboard
- สถิติรวม: ผู้ใช้ทั้งหมด, ทรัพย์สินทั้งหมด, สัญญา, รายได้
- รายงานที่รอแก้ไข
- ผู้ใช้ใหม่, ทรัพย์สินใหม่
- ทรัพย์สินยอดนิยม

#### 2. จัดการผู้ใช้ (Users)
- ดูรายการผู้ใช้ทั้งหมด (Buyer, Seller)
- ค้นหา, กรอง
- ระงับบัญชี, ลบบัญชี
- ดูโปรไฟล์ผู้ใช้

**การเก็บข้อมูลการระงับ:**
```javascript
localStorage.setItem('admin_moderation_log', JSON.stringify([
  {
    id: 'mod_1',
    issueId: 'issue_123',
    action: 'ban',
    reason: 'โพสต์เนื้อหาที่ไม่เหมาะสม',
    timestamp: '2025-01-15T10:30:00Z',
    adminId: 'admin_1'
  }
]));
```

#### 3. จัดการทรัพย์สิน (Properties)
- ดูรายการทรัพย์สินทั้งหมด
- ค้นหา, กรองตามประเภท, สถานะ
- อนุมัติ/ไม่อนุมัติประกาศ
- ลบประกาศ
- ดูรายละเอียดทรัพย์สิน

#### 4. รายงานปัญหา (Reports)
- ดูรายงานทั้งหมด
- กรองตามประเภท: ทรัพย์สิน, ผู้ใช้, ข้อความ
- แก้ไขปัญหา:
  - ระงับบัญชี
  - ลบโพสต์
  - ส่งข้อความเตือน
- ทำเครื่องหมายว่าแก้ไขแล้ว

**โครงสร้างข้อมูลรายงาน:**
```javascript
{
  id: 'report_1',
  type: 'property' | 'user' | 'message',
  reporterId: 123,
  targetId: 456,
  reason: 'เนื้อหาไม่เหมาะสม',
  details: '...',
  status: 'pending' | 'resolved',
  timestamp: '2025-01-15T10:30:00Z'
}
```

#### 5. สัญญา (Contracts)
- ดูสัญญาทั้งหมด
- กรองตามประเภท: ห้องเช่า, บ้านเช่า, อื่นๆ
- ดูรายละเอียดสัญญา
- ดาวน์โหลด PDF

#### 6. Analytics
- สถิติผู้ใช้: ผู้ใช้ใหม่, ผู้ใช้ที่ยืนยันแล้ว
- สถิติทรัพย์สิน: ทรัพย์สินใหม่, ประเภททรัพย์สิน
- สถิติสัญญา: สัญญาใหม่, สัญญาที่เสร็จสมบูรณ์
- กราฟแสดงแนวโน้ม

#### 7. Settings
- ตั้งค่าการโฆษณา
- ตั้งค่าระบบ (วันหมดอายุประกาศ, จำนวนประกาศสูงสุด)
- ตั้งค่าเมนูผู้ซื้อ
- จัดการโปรไฟล์ Admin

---

## 💾 การเก็บข้อมูล (Data Storage)

### 1. localStorage (ข้อมูลผู้ใช้)

**โครงสร้าง:**
```javascript
// Login State
'haatee_login_state' → { email, type, role, lastLogin }

// Seller Email
'sellerEmail' → 'seller@haatee.com'
'buyerEmail' → 'buyer@haatee.com'

// Profile
'profile_${email}' → { firstName, lastName, email, phone, ... }

// Saved Properties
'savedProperties_${buyerEmail}' → [propertyId1, propertyId2, ...]

// Chat Messages
'seller_chats_${sellerId}' → { 'property_123': { messages: [...] } }
'buyer_chat_${propertyId}_${sellerId}' → [message1, message2, ...]

// Contracts
'contracts_${buyerEmail}' → [{ id, propertyId, ... }]

// Admin Moderation
'admin_moderation_log' → [{ id, action, reason, ... }]
'admin_resolution_log' → [{ id, issueId, ... }]
```

### 2. sessionStorage (ข้อมูลชั่วคราว)

```javascript
// Current Page
'currentPage' → 'home' | 'buyer' | 'seller' | 'admin' | ...

// Search Parameters
'homeSearchParams' → { location, type, price, fromHome: true }
'previousPageFromHome' → 'home'
```

### 3. JSON Files (ข้อมูล Static)

- `properties.json` - ทรัพย์สินทั้งหมด (240,000+ รายการ)
- `users.json` - เจ้าของทรัพย์/นายหน้า
- `buyers.json` - ผู้ซื้อ
- `reports.json` - รายงานปัญหา
- `chatMessages.json` - ข้อความแชทเริ่มต้น

---

## 🔄 การทำงานของระบบ (Flow)

### 1. Flow การเข้าสู่ระบบ

```
1. ผู้ใช้เปิดเว็บ → App.jsx โหลด
2. ตรวจสอบ localStorage['haatee_login_state']
   - ถ้ามี → redirect ไป dashboard ตาม type
   - ถ้าไม่มี → แสดงหน้า Home
3. คลิก "เข้าสู่ระบบ" → ไปหน้า Login
4. เลือกประเภทผู้ใช้ → กรอกอีเมล/รหัสผ่าน
5. ตรวจสอบ credentials → ส่ง OTP
6. กรอก OTP (123456) → ยืนยันสำเร็จ
7. บันทึก login state → redirect ไป dashboard
```

### 2. Flow การค้นหาทรัพย์สิน

```
1. หน้า Home → กรอกข้อมูลค้นหา
2. คลิก "ค้นหา" → บันทึกพารามิเตอร์ใน sessionStorage
3. ไปหน้า Buyer → โหลดพารามิเตอร์จาก sessionStorage
4. กรองข้อมูลจาก properties.json
5. แสดงผลการค้นหา
6. คลิกทรัพย์สิน → ไปหน้า PropertyDetail
```

### 3. Flow การแชท

```
1. Buyer ดู PropertyDetail → คลิก "ติดต่อเจ้าของ"
2. ตรวจสอบ login → ถ้ายังไม่ login → แจ้งเตือน
3. สร้างแชทใหม่ → เก็บใน localStorage
4. Seller ดูข้อความใน Tab Chat
5. ตอบกลับ → บันทึกข้อความใหม่
6. Buyer ดูข้อความใน Tab Chat
```

### 4. Flow สัญญาดิจิทัล

```
1. Seller → Tab Contracts → สร้างสัญญาใหม่
2. เลือกทรัพย์สิน → กรอกข้อมูลผู้เช่า → กำหนดเงื่อนไข
3. ส่งสัญญาให้ผู้เช่า → ส่งผ่านแชท
4. Buyer รับสัญญาในแชท → ดูสัญญา → ลงนาม
5. Seller รับการแจ้งเตือน → ดาวน์โหลดสัญญาที่ลงนามแล้ว
```

---

## 🎨 การออกแบบ UI/UX

### Design System
- **สีหลัก**: Blue (#1976D2), Cyan (#00BCD4)
- **Icons**: lucide-react
- **Typography**: System fonts (Segoe UI, Tahoma, etc.)
- **Layout**: Responsive Design

### Component Structure
- **Header**: Logo, Navigation, User Menu
- **Sidebar**: เมนูหลัก (สำหรับ Seller/Admin)
- **Content Area**: แสดงเนื้อหาตาม Tab
- **Modal**: สำหรับฟอร์ม, แจ้งเตือน

---

## 🔧 Utility Functions

### 1. `generateSellerChats.js`
- สร้างข้อความแชทเริ่มต้นให้ Seller
- เรียกใช้ครั้งเดียวเมื่อเริ่มแอป
- เก็บใน localStorage

### 2. `contractPdf.js`
- สร้าง PDF สัญญา
- ใช้สำหรับดาวน์โหลดสัญญา

---

## 📝 ข้อควรระวัง

1. **ข้อมูลใน localStorage จะหายเมื่อ Clear Browser Data**
2. **JSON Files เป็น Static Data** - ไม่สามารถแก้ไขได้โดยตรง (ต้องแก้ไฟล์)
3. **OTP เป็นแบบจำลอง** - ใช้ `123456` สำหรับทุกคน
4. **ไม่มี Backend** - ข้อมูลทั้งหมดอยู่ฝั่ง Frontend
5. **ไม่มี Database** - ใช้ localStorage และ JSON files

---

## 🚀 การรันโปรเจค

```bash
# ติดตั้ง dependencies
npm install

# รัน development server
npm run dev

# Build สำหรับ production
npm run build

# Preview production build
npm run preview
```

---

## 📊 สรุปสถิติระบบ

- **ทรัพย์สิน**: 240,000+ รายการ
- **ผู้ใช้**: หลายพันคน (จาก JSON files)
- **ประเภททรัพย์สิน**: บ้านเดี่ยว, คอนโด, ทาวน์โฮม, ที่ดิน, อพาร์ทเม้นท์, อาคารพาณิชย์
- **ฟีเจอร์หลัก**: ค้นหา, บันทึก, แชท, สัญญาดิจิทัล, สถิติ

---

## 🎯 จุดเด่นของระบบ

1. ✅ **UI สวยงาม Modern** - ใช้ Gradient, Animation
2. ✅ **Responsive Design** - รองรับ Mobile, Tablet, Desktop
3. ✅ **ระบบแชท Real-time** - เก็บใน localStorage
4. ✅ **สัญญาดิจิทัล** - สำหรับสัญญาเช่า ≤3 ปี
5. ✅ **ระบบสถิติ** - Dashboard สำหรับ Seller และ Admin
6. ✅ **ระบบรายงาน** - Admin สามารถจัดการปัญหาได้
7. ✅ **ระบบค้นหาขั้นสูง** - ค้นหาตาม BTS/MRT, ฟิลเตอร์หลายแบบ

---

## 🔮 การพัฒนาต่อ

หากต้องการพัฒนาต่อ สามารถเพิ่ม:
- Backend API (Node.js, Express)
- Database (MongoDB, PostgreSQL)
- Real-time Chat (WebSocket, Socket.io)
- Payment Integration
- Email Service (สำหรับ OTP จริง)
- Image Upload (Cloud Storage)
- Search Engine (Elasticsearch)

---

**เอกสารนี้สรุปการทำงานของระบบ HaaTee ทั้งหมด หากมีคำถามเพิ่มเติม สามารถดูโค้ดในแต่ละไฟล์ได้เลย!** 🎉

